install.packages(c("fitdistrplus", "ggplot2", "readr"))
library(fitdistrplus)
library(ggplot2)
library(readr)
library(ggplot2)
library(readr)
# Importar datos
datos <- read_excel("Produccion.xlsx", sheet_name="Proporcion")
install.packages(c("fitdistrplus", "ggplot2", "readxl"))
install.packages(c("fitdistrplus", "ggplot2", "readxl"))
library(fitdistrplus)
library(ggplot2)
library(readxl)
# Importar datos
datos <- read_excel("Produccion.xlsx", sheet="Proporcion")
setwd("~/8vo semestre/Tesis")
# Importar datos
datos <- read_excel("Produccion.xlsx", sheet="Proporcion")
# Importar datos
datos <- read_excel("Produccion.xlsx", sheet="Proporcion")
# Verificar
head(datos)
summary(datos$proporcion)
mes <- ENERO
mes <- "ENERO"
# Verificar
head(datos)
summary(datos$mes)
summary(datos$ENERO)
ggplot(datos, aes(x = proporcion)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
mes <- datos$ENERO
ggplot(mes, aes(x = proporcion)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
ggplot(datos, aes(x = ENERO)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
# PRUEBAS DE AJUSTE ---------------------------------------
x <- datos$ENERO
# Normal
ajuste_norm <- fitdist(x, "norm")
# Exponencial
ajuste_exp <- fitdist(x, "exp")
# Beta (solo si 0 < x < 1)
ajuste_beta <- fitdist(x, "beta", start = list(shape1 = 2, shape2 = 2))
# Uniforme
ajuste_unif <- fitdist(x, "unif")
ajuste_norm
resumen <- data.frame(
Distribucion = c("Normal", "Exponencial", "Beta", "Uniforme"),
AIC = c(ajuste_norm$aic, ajuste_exp$aic, ajuste_beta$aic, ajuste_unif$aic),
BIC = c(ajuste_norm$bic, ajuste_exp$bic, ajuste_beta$bic, ajuste_unif$bic)
)
print(resumen)
# PRUEBAS DE AJUSTE KOLMOGOROV-SMIRNOV --------------------
# Normal
ks.test(x, "pnorm", mean = mean(x), sd = sd(x))
# Exponencial
ks.test(x, "pexp", rate = 1/mean(x))
# Beta
param_beta <- coef(ajuste_beta)
ks.test(x, "pbeta", shape1 = param_beta["shape1"], shape2 = param_beta["shape2"])
# Uniforme
ks.test(x, "punif", min = min(x), max = max(x))
# VISUALIZACION COMPARATIVA -------------------------------
par(mfrow = c(2, 2))
plot(ajuste_norm)
plot(ajuste_exp)
plot(ajuste_beta)
plot(ajuste_unif)
# AJUSTE --------------------------------------------------
x <- datos$FEBR
# PRUEBAS DE AJUSTE KOLMOGOROV-SMIRNOV --------------------
# Normal
ks.test(x, "pnorm", mean = mean(x), sd = sd(x))
# Exponencial
ks.test(x, "pexp", rate = 1/mean(x))
# Beta
param_beta <- coef(ajuste_beta)
ks.test(x, "pbeta", shape1 = param_beta["shape1"], shape2 = param_beta["shape2"])
# Uniforme
ks.test(x, "punif", min = min(x), max = max(x))
# VISUALIZACION DE LOS DATOS ------------------------------
ggplot(datos, aes(x = FEBR)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
# AJUSTE --------------------------------------------------
x <- datos$FEBR
# Normal
ajuste_norm <- fitdist(x, "norm")
# Exponencial
ajuste_exp <- fitdist(x, "exp")
# Beta (solo si 0 < x < 1)
ajuste_beta <- fitdist(x, "beta", start = list(shape1 = 2, shape2 = 2))
# Uniforme
ajuste_unif <- fitdist(x, "unif")
# VISUALIZACION COMPARATIVA -------------------------------
par(mfrow = c(2, 2))
plot(ajuste_norm)
plot(ajuste_exp)
plot(ajuste_beta)
plot(ajuste_unif)
ajuste_beta
resumen <- data.frame(
Distribucion = c("Normal", "Exponencial", "Beta", "Uniforme"),
AIC = c(ajuste_norm$aic, ajuste_exp$aic, ajuste_beta$aic, ajuste_unif$aic),
BIC = c(ajuste_norm$bic, ajuste_exp$bic, ajuste_beta$bic, ajuste_unif$bic)
)
print(resumen)
# PARAMETROS
ajuste_norm
summary(datos$MARZ)
# VISUALIZACION DE LOS DATOS ------------------------------
ggplot(datos, aes(x = MARZ)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
# AJUSTE --------------------------------------------------
x <- datos$MARZ
# Normal
ajuste_norm <- fitdist(x, "norm")
# Exponencial
ajuste_exp <- fitdist(x, "exp")
# Beta (solo si 0 < x < 1)
ajuste_beta <- fitdist(x, "beta", start = list(shape1 = 2, shape2 = 2))
# Uniforme
ajuste_unif <- fitdist(x, "unif")
# PRUEBAS DE AJUSTE KOLMOGOROV-SMIRNOV --------------------
# Normal
ks.test(x, "pnorm", mean = mean(x), sd = sd(x))
# Exponencial
ks.test(x, "pexp", rate = 1/mean(x))
# Beta
param_beta <- coef(ajuste_beta)
ks.test(x, "pbeta", shape1 = param_beta["shape1"], shape2 = param_beta["shape2"])
# Uniforme
ks.test(x, "punif", min = min(x), max = max(x))
# VISUALIZACION COMPARATIVA -------------------------------
par(mfrow = c(2, 2))
plot(ajuste_norm)
plot(ajuste_exp)
plot(ajuste_beta)
plot(ajuste_unif)
# PARAMETROS
ajuste_norm
resumen <- data.frame(
Distribucion = c("Normal", "Exponencial", "Beta", "Uniforme"),
AIC = c(ajuste_norm$aic, ajuste_exp$aic, ajuste_beta$aic, ajuste_unif$aic),
BIC = c(ajuste_norm$bic, ajuste_exp$bic, ajuste_beta$bic, ajuste_unif$bic)
)
print(resumen)
# VISUALIZACION DE LOS DATOS ------------------------------
ggplot(datos, aes(x = ABRIL)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
# AJUSTE --------------------------------------------------
x <- datos$ABRIL
# Normal
ajuste_norm <- fitdist(x, "norm")
# Exponencial
ajuste_exp <- fitdist(x, "exp")
# Beta (solo si 0 < x < 1)
ajuste_beta <- fitdist(x, "beta", start = list(shape1 = 2, shape2 = 2))
# Uniforme
ajuste_unif <- fitdist(x, "unif")
resumen <- data.frame(
Distribucion = c("Normal", "Exponencial", "Beta", "Uniforme"),
AIC = c(ajuste_norm$aic, ajuste_exp$aic, ajuste_beta$aic, ajuste_unif$aic),
BIC = c(ajuste_norm$bic, ajuste_exp$bic, ajuste_beta$bic, ajuste_unif$bic)
)
print(resumen)
# PRUEBAS DE AJUSTE KOLMOGOROV-SMIRNOV --------------------
# Normal
ks.test(x, "pnorm", mean = mean(x), sd = sd(x))
# Exponencial
ks.test(x, "pexp", rate = 1/mean(x))
# Beta
param_beta <- coef(ajuste_beta)
ks.test(x, "pbeta", shape1 = param_beta["shape1"], shape2 = param_beta["shape2"])
# Uniforme
ks.test(x, "punif", min = min(x), max = max(x))
# VISUALIZACION COMPARATIVA -------------------------------
par(mfrow = c(2, 2))
plot(ajuste_norm)
plot(ajuste_exp)
plot(ajuste_beta)
plot(ajuste_unif)
# PARAMETROS
ajuste_norm
# VISUALIZACION DE LOS DATOS ------------------------------
ggplot(datos, aes(x = MAYO)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
# AJUSTE --------------------------------------------------
x <- datos$MAYO
# Normal
ajuste_norm <- fitdist(x, "norm")
# Exponencial
ajuste_exp <- fitdist(x, "exp")
# Beta (solo si 0 < x < 1)
ajuste_beta <- fitdist(x, "beta", start = list(shape1 = 2, shape2 = 2))
# Uniforme
ajuste_unif <- fitdist(x, "unif")
resumen <- data.frame(
Distribucion = c("Normal", "Exponencial", "Beta", "Uniforme"),
AIC = c(ajuste_norm$aic, ajuste_exp$aic, ajuste_beta$aic, ajuste_unif$aic),
BIC = c(ajuste_norm$bic, ajuste_exp$bic, ajuste_beta$bic, ajuste_unif$bic)
)
print(resumen)
# PRUEBAS DE AJUSTE KOLMOGOROV-SMIRNOV --------------------
# Normal
ks.test(x, "pnorm", mean = mean(x), sd = sd(x))
# Exponencial
ks.test(x, "pexp", rate = 1/mean(x))
# Beta
param_beta <- coef(ajuste_beta)
ks.test(x, "pbeta", shape1 = param_beta["shape1"], shape2 = param_beta["shape2"])
# VISUALIZACION COMPARATIVA -------------------------------
par(mfrow = c(2, 2))
# Uniforme
ks.test(x, "punif", min = min(x), max = max(x))
plot(ajuste_exp)
plot(ajuste_norm)
plot(ajuste_beta)
plot(ajuste_unif)
# PARAMETROS
ajuste_norm
# VISUALIZACION DE LOS DATOS ------------------------------
ggplot(datos, aes(x = JUNIO)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
# AJUSTE --------------------------------------------------
x <- datos$JUNIO
# Normal
ajuste_norm <- fitdist(x, "norm")
# Exponencial
ajuste_exp <- fitdist(x, "exp")
# Beta (solo si 0 < x < 1)
ajuste_beta <- fitdist(x, "beta", start = list(shape1 = 2, shape2 = 2))
# Uniforme
ajuste_unif <- fitdist(x, "unif")
resumen <- data.frame(
Distribucion = c("Normal", "Exponencial", "Beta", "Uniforme"),
AIC = c(ajuste_norm$aic, ajuste_exp$aic, ajuste_beta$aic, ajuste_unif$aic),
BIC = c(ajuste_norm$bic, ajuste_exp$bic, ajuste_beta$bic, ajuste_unif$bic)
)
print(resumen)
# PRUEBAS DE AJUSTE KOLMOGOROV-SMIRNOV --------------------
# Normal
ks.test(x, "pnorm", mean = mean(x), sd = sd(x))
# Exponencial
ks.test(x, "pexp", rate = 1/mean(x))
# Beta
param_beta <- coef(ajuste_beta)
ks.test(x, "pbeta", shape1 = param_beta["shape1"], shape2 = param_beta["shape2"])
# Uniforme
ks.test(x, "punif", min = min(x), max = max(x))
# VISUALIZACION COMPARATIVA -------------------------------
par(mfrow = c(2, 2))
plot(ajuste_norm)
plot(ajuste_exp)
plot(ajuste_beta)
plot(ajuste_unif)
# PARAMETROS
ajuste_norm
# VISUALIZACION DE LOS DATOS ------------------------------
ggplot(datos, aes(x = JULIO)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
# AJUSTE --------------------------------------------------
x <- datos$JULIO
# Normal
ajuste_norm <- fitdist(x, "norm")
# Exponencial
ajuste_exp <- fitdist(x, "exp")
# Beta (solo si 0 < x < 1)
ajuste_beta <- fitdist(x, "beta", start = list(shape1 = 2, shape2 = 2))
# Uniforme
ajuste_unif <- fitdist(x, "unif")
resumen <- data.frame(
Distribucion = c("Normal", "Exponencial", "Beta", "Uniforme"),
AIC = c(ajuste_norm$aic, ajuste_exp$aic, ajuste_beta$aic, ajuste_unif$aic),
BIC = c(ajuste_norm$bic, ajuste_exp$bic, ajuste_beta$bic, ajuste_unif$bic)
)
print(resumen)
# PRUEBAS DE AJUSTE KOLMOGOROV-SMIRNOV --------------------
# Normal
ks.test(x, "pnorm", mean = mean(x), sd = sd(x))
# Exponencial
ks.test(x, "pexp", rate = 1/mean(x))
# Beta
param_beta <- coef(ajuste_beta)
ks.test(x, "pbeta", shape1 = param_beta["shape1"], shape2 = param_beta["shape2"])
# Uniforme
ks.test(x, "punif", min = min(x), max = max(x))
# VISUALIZACION COMPARATIVA -------------------------------
par(mfrow = c(2, 2))
plot(ajuste_norm)
plot(ajuste_exp)
plot(ajuste_beta)
plot(ajuste_unif)
# PARAMETROS
ajuste_norm
ajuste_beta
# VISUALIZACION DE LOS DATOS ------------------------------
ggplot(datos, aes(x = AGOT)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
# AJUSTE --------------------------------------------------
x <- datos$JULIO
# AJUSTE --------------------------------------------------
x <- datos$AGOT
# Normal
ajuste_norm <- fitdist(x, "norm")
# Exponencial
ajuste_exp <- fitdist(x, "exp")
# Beta (solo si 0 < x < 1)
ajuste_beta <- fitdist(x, "beta", start = list(shape1 = 2, shape2 = 2))
# Uniforme
ajuste_unif <- fitdist(x, "unif")
resumen <- data.frame(
Distribucion = c("Normal", "Exponencial", "Beta", "Uniforme"),
AIC = c(ajuste_norm$aic, ajuste_exp$aic, ajuste_beta$aic, ajuste_unif$aic),
BIC = c(ajuste_norm$bic, ajuste_exp$bic, ajuste_beta$bic, ajuste_unif$bic)
)
print(resumen)
# PRUEBAS DE AJUSTE KOLMOGOROV-SMIRNOV --------------------
# Normal
ks.test(x, "pnorm", mean = mean(x), sd = sd(x))
# Exponencial
ks.test(x, "pexp", rate = 1/mean(x))
# Beta
param_beta <- coef(ajuste_beta)
ks.test(x, "pbeta", shape1 = param_beta["shape1"], shape2 = param_beta["shape2"])
# Uniforme
ks.test(x, "punif", min = min(x), max = max(x))
# VISUALIZACION COMPARATIVA -------------------------------
par(mfrow = c(2, 2))
plot(ajuste_norm)
plot(ajuste_exp)
plot(ajuste_beta)
plot(ajuste_unif)
# PARAMETROS
ajuste_norm
# VISUALIZACION DE LOS DATOS ------------------------------
ggplot(datos, aes(x = SEPT)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
# AJUSTE --------------------------------------------------
x <- datos$SEPT
# Normal
ajuste_norm <- fitdist(x, "norm")
# Exponencial
ajuste_exp <- fitdist(x, "exp")
# Beta (solo si 0 < x < 1)
ajuste_beta <- fitdist(x, "beta", start = list(shape1 = 2, shape2 = 2))
# Uniforme
ajuste_unif <- fitdist(x, "unif")
resumen <- data.frame(
Distribucion = c("Normal", "Exponencial", "Beta", "Uniforme"),
AIC = c(ajuste_norm$aic, ajuste_exp$aic, ajuste_beta$aic, ajuste_unif$aic),
BIC = c(ajuste_norm$bic, ajuste_exp$bic, ajuste_beta$bic, ajuste_unif$bic)
)
print(resumen)
# PRUEBAS DE AJUSTE KOLMOGOROV-SMIRNOV --------------------
# Normal
ks.test(x, "pnorm", mean = mean(x), sd = sd(x))
# Exponencial
ks.test(x, "pexp", rate = 1/mean(x))
# Beta
param_beta <- coef(ajuste_beta)
ks.test(x, "pbeta", shape1 = param_beta["shape1"], shape2 = param_beta["shape2"])
# Uniforme
ks.test(x, "punif", min = min(x), max = max(x))
# VISUALIZACION COMPARATIVA -------------------------------
par(mfrow = c(2, 2))
plot(ajuste_exp)
plot(ajuste_beta)
plot(ajuste_norm)
plot(ajuste_unif)
plot(ajuste_norm)
plot(ajuste_beta)
# PARAMETROS
ajuste_norm
# VISUALIZACION DE LOS DATOS ------------------------------
ggplot(datos, aes(x = OCT)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
# AJUSTE --------------------------------------------------
x <- datos$OCT
# Normal
ajuste_norm <- fitdist(x, "norm")
# Exponencial
ajuste_exp <- fitdist(x, "exp")
# Beta (solo si 0 < x < 1)
ajuste_beta <- fitdist(x, "beta", start = list(shape1 = 2, shape2 = 2))
# Uniforme
ajuste_unif <- fitdist(x, "unif")
resumen <- data.frame(
Distribucion = c("Normal", "Exponencial", "Beta", "Uniforme"),
AIC = c(ajuste_norm$aic, ajuste_exp$aic, ajuste_beta$aic, ajuste_unif$aic),
BIC = c(ajuste_norm$bic, ajuste_exp$bic, ajuste_beta$bic, ajuste_unif$bic)
)
print(resumen)
# PRUEBAS DE AJUSTE KOLMOGOROV-SMIRNOV --------------------
# Normal
ks.test(x, "pnorm", mean = mean(x), sd = sd(x))
# Exponencial
ks.test(x, "pexp", rate = 1/mean(x))
# Beta
param_beta <- coef(ajuste_beta)
ks.test(x, "pbeta", shape1 = param_beta["shape1"], shape2 = param_beta["shape2"])
# Uniforme
ks.test(x, "punif", min = min(x), max = max(x))
plot(ajuste_norm)
plot(ajuste_beta)
plot(ajuste_exp)
# PARAMETROS
ajuste_norm
# VISUALIZACION DE LOS DATOS ------------------------------
ggplot(datos, aes(x = NOV)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
# AJUSTE --------------------------------------------------
x <- datos$NOV
# Normal
ajuste_norm <- fitdist(x, "norm")
# Exponencial
ajuste_exp <- fitdist(x, "exp")
# Beta (solo si 0 < x < 1)
ajuste_beta <- fitdist(x, "beta", start = list(shape1 = 2, shape2 = 2))
# Uniforme
ajuste_unif <- fitdist(x, "unif")
resumen <- data.frame(
Distribucion = c("Normal", "Exponencial", "Beta", "Uniforme"),
AIC = c(ajuste_norm$aic, ajuste_exp$aic, ajuste_beta$aic, ajuste_unif$aic),
BIC = c(ajuste_norm$bic, ajuste_exp$bic, ajuste_beta$bic, ajuste_unif$bic)
)
print(resumen)
# PRUEBAS DE AJUSTE KOLMOGOROV-SMIRNOV --------------------
# Normal
ks.test(x, "pnorm", mean = mean(x), sd = sd(x))
# Exponencial
ks.test(x, "pexp", rate = 1/mean(x))
# Beta
param_beta <- coef(ajuste_beta)
ks.test(x, "pbeta", shape1 = param_beta["shape1"], shape2 = param_beta["shape2"])
# Uniforme
ks.test(x, "punif", min = min(x), max = max(x))
# PARAMETROS
ajuste_norm
plot(ajuste_norm)
# VISUALIZACION DE LOS DATOS ------------------------------
ggplot(datos, aes(x = DIC)) +
geom_histogram(aes(y = ..density..), bins = 10, fill = "lightblue", color = "black") +
geom_density(color = "red", linewidth = 1) +
labs(title = "Distribución de la proporción mensual de producción")
# AJUSTE --------------------------------------------------
x <- datos$DIC
# Normal
ajuste_norm <- fitdist(x, "norm")
# Exponencial
ajuste_exp <- fitdist(x, "exp")
# Beta (solo si 0 < x < 1)
ajuste_beta <- fitdist(x, "beta", start = list(shape1 = 2, shape2 = 2))
# Uniforme
ajuste_unif <- fitdist(x, "unif")
resumen <- data.frame(
Distribucion = c("Normal", "Exponencial", "Beta", "Uniforme"),
AIC = c(ajuste_norm$aic, ajuste_exp$aic, ajuste_beta$aic, ajuste_unif$aic),
BIC = c(ajuste_norm$bic, ajuste_exp$bic, ajuste_beta$bic, ajuste_unif$bic)
)
print(resumen)
# PRUEBAS DE AJUSTE KOLMOGOROV-SMIRNOV --------------------
# Normal
ks.test(x, "pnorm", mean = mean(x), sd = sd(x))
# Exponencial
ks.test(x, "pexp", rate = 1/mean(x))
# Beta
param_beta <- coef(ajuste_beta)
ks.test(x, "pbeta", shape1 = param_beta["shape1"], shape2 = param_beta["shape2"])
# Uniforme
ks.test(x, "punif", min = min(x), max = max(x))
# PARAMETROS
ajuste_norm
